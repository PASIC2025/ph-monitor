// *** CRITICAL CHANGE: The API is now on the same domain, so use a relative path. ***
// Render will handle the domain and port for you.
const API_URL = '/api/ph-data'; 
const POLLING_INTERVAL_MS = 10000; // Poll every 10 seconds

// ... rest of the Chart.js and fetchDataAndUpdateChart function code ...